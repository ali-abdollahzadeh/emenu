specVersion: "v1"
serviceId: "emenu"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "emenu-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
